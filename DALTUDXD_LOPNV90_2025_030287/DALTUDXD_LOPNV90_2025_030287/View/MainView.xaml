<Window x:Class="DALTUDXD_LOPNV90_2025_030287.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DALTUDXD_LOPNV90_2025_030287.View"
        mc:Ignorable="d"
        Title="MainView" Height="479" Width="926" Background="#FF2B97A0">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <!-- Thiết Kế -->
        <GroupBox Header="Thiết Kế" Grid.Row="0" Grid.Column="0" Margin="5">
            <StackPanel Margin="10,0,10,0" Height="139" Background="White">
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Tên loại gạch" Width="100"/>
                    <TextBox Name="txtTenLoaiGach" Width="150" TextChanged="txtTenLoaiGach_TextChanged">
                        <TextBox.Background>
                            <ImageBrush/>
                        </TextBox.Background>
                    </TextBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Kích thước" Width="100"/>
                    <ComboBox Name="cbKichThuoc" Width="150"/>
                    <Label Content="mm" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Giá gạch" Width="100"/>
                    <TextBox Name="txtGiaGach" Width="150"/>
                    <Label Content="Đồng" Margin="5,0,0,0"/>
                </StackPanel>

                <Button Content="Kiểm tra kho" Width="106" Margin="0,10,0,0" RenderTransformOrigin="0.494,-0.56" Height="19"/>
            </StackPanel>
        </GroupBox>

        <!-- Số gạch trong kho -->
        <GroupBox Header="Số gạch trong kho" Grid.Row="0" Grid.Column="1" Margin="5">
            <DataGrid Name="dgKho" AutoGenerateColumns="False" IsReadOnly="True" Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tên loại gạch" Binding="{Binding TenLoaiGach}"/>
                    <DataGridTextColumn Header="Số mảnh tái sử dụng" Binding="{Binding SoManhTaiSuDung}"/>
                    <DataGridTextColumn Header="Kích thước còn lại" Binding="{Binding KichThuocConLai}"/>
                    <DataGridTextColumn Header="Mảnh dư còn lại trong kho" Binding="{Binding ManhDuConLai}"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Bảng dữ liệu dưới -->
        <DataGrid Name="dgSan" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
               AutoGenerateColumns="False" Margin="5" SelectionChanged="dgSan_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Số thứ tự" Binding="{Binding SoThuTu}"/>
                <DataGridTextColumn Header="Tầng" Binding="{Binding Tang}"/>
                <DataGridTextColumn Header="Diện tích sàn" Binding="{Binding DienTichSan}"/>
                <DataGridTextColumn Header="Độ dày sàn" Binding="{Binding DoDaySan}"/>
                <DataGridTextColumn Header="Tên loại gạch" Binding="{Binding TenLoaiGach}"/>
                <DataGridTextColumn Header="Kích thước" Binding="{Binding KichThuoc}"/>
                <DataGridTextColumn Header="Số lượng gạch" Binding="{Binding SoLuongGach}"/>
                <DataGridTextColumn Header="Chi phí" Binding="{Binding ChiPhi}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
